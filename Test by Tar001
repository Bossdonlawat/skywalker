#include <stdio.h>
void main()
{
    int n,h;
    scanf("%d",&n);
    int a[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(int i=0;i<n;i++)
    {
        if(a[i]==a[i+1])
        {
            h=a[i];
        }
    }
    printf("%d",h);
}
